class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        while True:
            try:
                nums.pop(nums.index(val))
            except:
                break

        return len(nums)
